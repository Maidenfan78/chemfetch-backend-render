# Example env for backend (Node.js API)
# Copy to .env for local runs. In Render, set these in the dashboard.

# Server
PORT=3001

# Supabase (Service Role)
SB_URL=https://your-project-ref.supabase.co
SB_SERVICE_KEY=your-service-role-key

# OCR service (local-only; Render uses internal service URL from render.yaml)
EXPO_PUBLIC_OCR_API_URL=http://127.0.0.1:5001

# Google Programmable Search (optional but recommended)
GOOGLE_SEARCH_API_KEY=
GOOGLE_SEARCH_ENGINE_ID=

# CORS allowed origins (comma-separated)
FRONTEND_URL=http://localhost:3000
